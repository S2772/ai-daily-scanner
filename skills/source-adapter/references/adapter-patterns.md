# Adapter Patterns

- Normalize all parsed outputs to one dict shape.
- Prefer explicit defaults over missing keys.
- Keep per-source parser isolated in dedicated method/function.
- Log source URL and exception type for failures.
- Avoid broad category/tag assumptions in parser layer.
